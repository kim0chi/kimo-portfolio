import type { Config } from "tailwindcss";

const config: Config = {
  // Tailwind v4 - Most configuration now lives in CSS via @theme
  // Only essential config options remain here
  darkMode: "class",
};

export default config;
